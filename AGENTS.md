# kaname-shogi の作業方針

## 目的

将棋のルールを学びながら、理解できる小さな段階で自作の将棋プログラムを育てる。強さや完成速度よりも、各処理が必要な理由を説明できることを優先する。

## 学習の進め方

- 学習は一回一テーマで進める。
- 各回では、`docs/learning/` に学習の目的・要点・確認問題・回答・振り返り・次回への問いを記録する。
- 実装に関係する確定知識は、`docs/knowledge/` に簡潔な参照メモとして記録する。
- ルールの確認には日本将棋連盟などの一次情報を優先し、文書内に出典リンクを添える。
- 学習記録・知識メモは内容を確認してからGitにコミットする。
- 確認問題は一度に一問ずつ出し、本人の回答を待ってから説明する。本人の回答とアシスタントの補足を区別して記録し、未回答を正解として記録しない。
- 英語の型名やメソッド名は、日本語の意味と「データか操作か」を併せて説明する。例えば `Square` はマスを表す値であり、「指す」「置く」という操作ではない。

## 開発の進め方

- 関連するルールを学んだ後に、その範囲だけを最小実装する。
- セッション再開時は、現在のGit状態とREADMEから最新の学習・設計記録を確認する。過去の引き継ぎに書かれたコミットやテスト結果を、現在の状態と同一だと決めつけない。
- 開発時は、独自拡張版の `superpowerssuperpowers` プラグインに含まれるスキルの適用範囲を確認して従う。機能追加・修正では、対象の振る舞いごとにテストの失敗理由を確認し、最小実装、成功確認、Refactorの要否確認を行う。読み込みエラーだけで振る舞いのRed確認を完了したと扱わない。スキルが利用できない場合も、この方針は守り、利用状況を正確に伝える。
- 実装は原則として `main` ではなく、目的が分かる作業用ブランチで行う。`main` への取り込みは、テーマの実装・検証・レビュー・記録がそろい、本人の承認を得てから行う。
- Refactorの要否確認後は、変更の有無にかかわらず独立したコードレビューを行い、Critical・Important・Minorの結論と対応を学習記録へ残す。CriticalまたはImportantがあれば解消して再検証・再レビューしてから、文書更新とコミットへ進む。
- 未決定の技術選択や将来の機能を先取りして複雑にしない。
- 実装前に、対象範囲・表現・確認方法を明確にする。
- 実装後は、変更に見合う検証を行ってからコミットする。
- 設計・実装の文書は、作成タイミングや独立した文書の数よりも、記録に抜け漏れがなく、後から経緯を振り返れることを重視する。設計の判断理由、合意した範囲、実装手順と実際の変更、検証結果、未解決事項を設計書・実装記録・学習記録などに残し、参照先を明確にする。
- 実装計画を独立した文書にするか、設計書などにまとめるかは規模に応じて選ぶ。過去の別機能の文書があることだけを理由に、今回の記録を省略しない。事後に整理した内容は事後記録と明記し、実施していない手順や承認を実施済みとして記録しない。記録時期の柔軟性は、実装前の対象範囲・表現・確認方法の明確化や、必要な承認・検証を省略する意味ではない。
- 座標は将棋の筋・段を基本とする。数学のxyは理解の補助であり、内部座標への希望と解釈しない。確定した保存方式は設計書を参照する。
- 別のGit作業ツリーで作業する場合は、実際の絶対パスと実行コマンドを案内する。元のディレクトリへの取り込み状況も明示する。
- 一テーマの実装・検証・レビュー・記録が揃った時点で、元の作業ディレクトリへの取り込み状況を確認する。必要な承認を得て取り込み、取り込み先で検証する。実装後の理解確認が残っている場合は、新機能へ進む前にその確認を再開する。
- 一テーマを完了して取り込み先での検証まで終えたら、最後の理解確認問題を一問だけ出し、本人の回答を待つ。回答と補足を学習記録へ追記してから、アシスタントは `docs/next-topics.md`、README、プロジェクトの方向性を見直し、次の候補を小さい順に示して推薦理由を説明する。本人が次テーマを選ぶまで、新しい学習・実装には進まない。
- 本人が次テーマを選び、新しいセッションで始めることを希望したら、`superpowerssuperpowers:session-handoff` スキルを使用する。選定理由、完了事項と背景、Git状態・作業ディレクトリ・直近の検証、次に読むファイル、再開用プロンプトを、`docs/handover-<topic>.md` と `docs/resume.md` に記録してGitへコミットする。次のセッションでは、その引き継ぎと現在の状態を確認してから一次資料の確認を始める。
- 次テーマを選んだ後に新しいセッションで進める場合、人間が再開用プロンプトを新しいセッションへ入力する。アシスタントは入力すべきプロンプトを明示し、人間の入力前に次テーマの学習・実装を開始しない。

## 文書と言語

- 説明・学習記録・知識メモは日本語で書く。
- 将来の開発・保守で判断を思い出せるよう、公開インターフェースのdocstringに引数・戻り値・副作用・前提条件と設計理由を記録する。処理を変えたら説明も更新する。
- 英語の駒名は、定義箇所で日本語名を併記する（例：`ROOK` は飛車、`BISHOP` は角行）。
- テストメソッド名は英語とし、日本語docstringの先頭行に確認する振る舞い、続く本文に検出したい誤りや背景を書く。標準の `unittest -v` で日本語の説明を表示する。
- このリポジトリの目的と方針は `README.md`、`docs/01-project-background.md`、`docs/02-project-direction.md` を基準にする。
