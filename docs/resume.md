# 学習・開発の再開案内

最終整理：2026-09-14。ここは再開位置の案内であり、恒常的な運用方針はルートのAGENTS.mdを参照する。テーマが進んだら本書も更新する。

## 現在の到達点

初期配置CLIと歩の移動先候補を実装済み。元の `/Users/oki2a24/kaname-shogi` のmainへ `4b3de93` まで取り込み、その時点で24テスト成功を確認した。その後の文書整理はGit履歴を参照する。現在のブランチ・変更・コミットは再開時に確認する。

歩の候補は盤面と出発マスを受け、所有者を盤上のPieceから読む。盤外・自駒は空リスト、空または歩以外の出発点はValueError。候補計算は盤面を変更せず、手番で制限しない。成り、合法手確定、実際の移動、持ち駒は未実装。

## 読む順序

1. ルートのAGENTS.mdと[README](../README.md)。
2. [歩の候補生成の設計](design/02-pawn-move-candidates.md)。
3. [第7回の理解・実装・監査の記録](learning/07-pawn-candidates-implementation.md)。
4. [候補生成のコード](../kaname_shogi/movegen.py)と[テスト](../tests/test_movegen.py)。

## 次に行うこと

新機能を実装せず、まず現在のコードの理解を確認する。最初の質問は「出発マスが空ならValueError、一歩前が盤外なら空リストを返すのはなぜか」。回答はまだ得ていないので、一問だけ出して待つ。理解確認後に次の一テーマを相談し、設計を確認してから実装する。

## 再開用プロンプト

```text
kaname-shogiの続きをお願いします。
AGENTS.mdとdocs/resume.mdを読み、現在のGit状態を確認してください。
再開案内に挙げた設計・学習記録・コード・テストを読んだうえで、
記載された最初の確認問題を一問だけ出し、私の回答を待ってください。
今回はまだ新機能の実装を始めないでください。
```

## 実行場所とコマンド

```sh
cd /Users/oki2a24/kaname-shogi
python3 -m unittest discover -s tests -v
python3 -m kaname_shogi
```

別の作業ツリーを選ぶ場合は、実際のパスと元のmainとの関係を確認する。
