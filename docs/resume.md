# 学習・開発の再開案内

最終整理：2026-09-22。恒常的な運用方針はルートのAGENTS.mdを参照する。

## 現在の到達点

玉・歩・金・銀・桂・香・飛車・角について、盤上の駒の所有者に応じた移動先候補を計算できる。候補計算は盤面・手番・持ち駒を変更せず、盤外と自駒を除外し、相手駒のあるマスを候補に含める。

`Position` は盤面、手番、先手の `sente_hand`、後手の `gote_hand` を持つ。`Hand` は一方の持ち駒の枚数を扱う可変のデータで、先後・盤面・手番は知らない。玉は持ち駒に入れない。

`move_piece(board, source, destination)` は空マスへの盤面操作だけを担う。`apply_move(position, source, destination)` は出発駒の所有者が手番と一致し、既存候補に含まれるときだけ局面を進める。到着マスが空なら移動し、相手の玉以外なら取り、手番側の持ち駒へ基本駒種を加える。玉取り、候補外、所有者不一致、空出発、自駒の到着、同一マスでは `ValueError` とし、盤面・手番・双方の持ち駒を変えない。

持ち駒を打つ操作、打ち場所の制限、成り・不成、成駒を取ったときの基本駒種への復元、王手・詰み・合法手、CLI入力、履歴、評価、探索は未実装である。

## 読む順序

1. ルートのAGENTS.md、[README](../README.md)、現在のGit状態。
2. [第24回](learning/24-candidate-only-empty-square-move.md)と、その設計・実装計画。
3. [第25回：駒取りと持ち駒の基礎](learning/25-capture-and-hands.md)、[参照メモ](knowledge/18-capture-and-hands.md)、[設計](design/13-capture-and-hands.md)、[実装計画](plans/2026-09-22-capture-and-hands.md)。
4. [移動・候補生成のコード](../kaname_shogi/movegen.py)、[状態モデル](../kaname_shogi/model.py)、[テスト](../tests/test_model.py)と[移動テスト](../tests/test_movegen.py)。

## 直近までの記録

第25回では、５五の先手歩が５四の後手歩を取る問いを通じ、５四の先手歩は盤上にあり、取られた歩は盤上にない先手の持ち駒であることを確認した。最初は持ち駒の歩を５四にあると答えたが、補足後の再確認では正しく区別できた。振り返りは「ありません」。

設計合意後にRed → Green → Refactorで実装した。`Hand` の内部状態を生成引数から渡せないようにし、駒取り・玉取り拒否・失敗時の双方の全基本駒種の不変性をテストした。最終レビューはCritical/Importantなしで、docstringの副作用説明を補足して反映した。実装後理解確認では、取った銀が先手の持ち駒となり盤上からなくなると正答し、振り返りは「引っかかっている点はない」だった。全106テスト、CLI、`git diff --check`を確認した。詳しくは[第25回](learning/25-capture-and-hands.md)を参照する。

## 次に行うこと

1. 次テーマ「持ち駒を打つ操作と打ち場所の制限」を、一次情報の確認から始める。
2. 確認問題は一度に一問だけ出し、本人の回答・補足・振り返りを記録する。
3. 設計合意まではコードを書かない。

## 再開用プロンプト

```text
kaname-shogiの続きをお願いします。
AGENTS.md、README.md、docs/resume.mdから最新の学習・設計・実装記録とGit状態を確認してください。
第25回「駒取りと持ち駒の基礎」は、実装・検証・レビュー・実装後理解確認まで完了しています。次テーマ「持ち駒を打つ操作と打ち場所の制限」の一次資料確認から始め、確認問題は一問ずつ私の回答を待ってください。設計承認前にコードを書かないでください。
```

## 実行場所とコマンド

```sh
cd /Users/oki2a24/kaname-shogi
python3 -m unittest discover -s tests -v
python3 -m kaname_shogi
```

過去の成功記録を今回実行済みとは扱わない。別の作業ツリーを選ぶ場合は、実際のパスと元のmainとの関係を確認する。
