# 飛車の移動先候補 実装計画

> **AIエージェントへの指示:** REQUIRED SUB-SKILL: この計画をタスクごとに実装するには、移植された `subagent-driven-development` スキル（推奨）または `executing-plans` スキルを起動して使用してください。ステップには追跡用のチェックボックス (`- [ ]`) を使用します。

**目標:** 成る前の飛車について、盤面を変更せず、縦横4方向の移動先候補を固定順で返す操作を追加する。

**アーキテクチャ:** 既存の `kaname_shogi.movegen` に飛車専用の公開関数を追加し、方向順「右 → 左 → 前 → 後ろ」で各方向を近い順に走査する。香との共通化は行わず、方向ごとの筋・段の増減を同じ処理へ渡す。

**技術スタック:** Python 3.9.6、標準ライブラリ、`unittest`。

**仕様 (Spec):** `docs/design/06-rook-move-candidates.md`

**グローバル制約 (Global Constraints):**
- 説明・記録は日本語で書く。
- Python標準ライブラリとunittestのみを使用する。
- 座標は将棋の筋・段を基本とする。
- 候補生成は盤面を変更せず、手番・成り・王手・合法手判定を扱わない。
- TDDはRed → Green → Refactorで進め、各段階の実施結果を正確に記録する。

---

## 変更ファイル

- 変更：`tests/test_movegen.py` — `RookMoveCandidatesTests` と飛車の振る舞いのテストを追加。
- 変更：`kaname_shogi/movegen.py` — `rook_move_candidates(board: Board, source: Square) -> list[Square]` とdocstringを追加。
- 変更：`docs/learning/15-rook-candidates-implementation.md` — 実際のRed・Green・Refactor、検証、レビューを記録。
- 変更：`README.md` — 飛車の公開関数と例を追記。

## 実装タスク

### タスク1：基準確認と失敗テスト

- [x] 現在のGit状態を確認する。
- [x] `python3 -m unittest discover -s tests -v` を実行し、既存54テストの現在の成功を確認する。
- [x] `tests/test_movegen.py` に、空または飛車以外を `ValueError` とするテストを追加する。テストメソッド名は英語、docstringは日本語にする。
- [x] `rook_move_candidates` が未定義の状態で対象テストを実行し、ImportErrorなどの準備段階と、対象の振る舞いのRedを区別して記録する。

### タスク2：開いた盤面の4方向をGreenにする

- [x] ５五の先手飛車が、右（４五・３五・２五・１五）、左（６五・７五・８五・９五）、前（５四・５三・５二・５一）、後ろ（５六・５七・５八・５九）の順で16候補を返すテストを追加する。
- [x] `movegen.py` に出発点の飛車検証、先後に応じた前後の段増減、4方向の筋・段増減、方向ごとの走査、空マスの追加を最小実装する。
- [x] 対象テストを実行し、Greenを確認する。

### タスク3：停止条件と境界を確認する

- [x] 各方向について、自駒の手前で止まり、自駒を含めず、その先も含めないテストを追加する。
- [x] 各方向について、相手駒のマスを最後の候補に含め、その先を含めないテストを追加する。
- [x] 右方向が塞がっていても左・前・後ろを調べること、筋・段の盤外で止まること、先後の両方を確認するテストを追加する。
- [x] 対象テストを実行し、停止条件と境界のRedを確認してから、最小実装でGreenにする。

### タスク4：契約の検証とRefactor

- [x] 候補計算後も全81マスが変わらないテストを追加する。
- [x] 呼び出しごとに独立したリストを返すテストを追加する。
- [x] 手番を変更しても出発駒の所有者から候補を計算するテストを追加する。
- [x] `python3 -m unittest discover -s tests -v` を実行し、全テスト成功を確認する。
- [x] Green後に、方向データと走査処理の重複、変数名、docstringの契約記載を点検する。整理が必要なら変更して再テストし、不要なら理由を実装記録に残す。香との共通化は行わない。

### タスク5：文書・CLI・レビュー・確定

- [x] `README.md` に `rook_move_candidates` の意味、候補順、相手駒・自駒の扱い、対象外を日本語で追記する。
- [x] `python3 -m kaname_shogi` を実行し、CLIが正常終了することを確認する。
- [x] `git diff --check` と文書内リンクの存在を確認する。
- [x] 読み取り専用レビューを行い、指摘の有無を記録する。
- [ ] 学習・設計・実装・検証・レビューの変更を確認してコミットする。
