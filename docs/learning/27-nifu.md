# 第27回：二歩

実施日：2026-09-22

## 目的

持ち駒の歩を打つとき、同じ筋に自分の未成の歩を二枚置けない二歩を理解し、駒打ちの操作に最小の制限として加える範囲を決める。

## ルールの確認

[日本将棋連盟「反則について」](https://www.shogi.or.jp/knowledge/shogi/05.php)は、同じ筋に歩があるとき、もう一枚の歩を打つことはできず、その状態を二歩と説明している。また、成った歩である「と」と同じ筋に歩を打つことは二歩ではないとしている。

持ち駒を原則として好きな所へ打てることと、占有マス・行き所のない所へ打てないことは、[日本将棋連盟「駒の動かし方」](https://www.shogi.or.jp/knowledge/shogi/02.php)を参照する。

### 第1問

先手の持ち駒に歩が1枚あり、盤上の５筋に先手の歩がすでに1枚あるとする。先手はその持ち歩を、空いている５五へ打てるか。

本人の回答：打てない。打ったら反則負け。

補足：正しい。同じ筋に自分の未成の歩がある状態で、さらに持ち歩を打つと二歩という反則になる。プログラムでは、その局面変更を始める前に打ちを拒否する。

### 第2問

先手の持ち駒に歩が1枚あり、５筋には先手のと金だけが置かれ、先手の未成の歩はないとする。このとき、空いている５五へ持ち歩を打てるか。

本人の回答：できる。と金と歩は同じ筋に共存できる。

補足：正しい。二歩の対象は未成の歩であり、と金は成った歩なので対象ではない。

### 第3問

先手の持ち駒に歩が1枚あり、５筋に先手の未成の歩があるため、先手が空いている５五へ歩を打とうとした。プログラムはこの打ちを拒否するとき、盤面・持ち駒・手番をどう扱うべきか。

本人の回答：盤面・持ち駒・手番、いずれも変更しない。

補足：正しい。二歩は打ちが成立しないため、盤面81マス、先後双方の持ち駒、手番を全て変更しない。

## 設計の合意

`apply_drop(position, piece_type, destination)` の公開インターフェースは変えず、`PAWN` を打つときだけ、打ち先と同じ筋に手番側の未成の歩があるかを調べる。該当すれば `ValueError` で拒否し、持ち駒の減算、盤面への配置、手番交代を行わない。

判定は、盤面を変更せず「指定側の未成の歩が指定筋にあるか」だけを返す非公開操作に分ける。歩以外の駒打ち、行き所のない駒、打ち歩詰め、王手・合法手は扱わない。現在は成り状態が未実装のため、`PieceType.PAWN` を未成の歩として判定する。将来、と金を表現したときに二歩判定から外す。

詳細は[二歩の設計](../design/15-nifu.md)を参照する。

## 実装記録

### Red

`ApplyDropTests` に、先手・後手それぞれで同じ５筋に自分の歩がある場合の持ち歩打ちを拒否し、局面を不変に保つテストを追加した。既存実装では歩を５五へ打てたため、2つのsubtestはいずれも `AssertionError: ValueError not raised` で失敗した。読み込みエラーではなく、二歩を拒否する振る舞いが未実装であることを確認した。

同じ筋に自分の歩があっても銀の打ちは成功するテストも追加し、二歩を歩以外へ広げないことを確認した。

### Green と Refactor

- `_has_unpromoted_pawn_on_file(board, side, file)` を追加した。打ち先の筋にある1段から9段を読み、指定側の `PieceType.PAWN` があれば `True` を返す非公開の判定操作である。
- `apply_drop` は、玉・占有マスの既存検証後、持ち駒を減らす前に歩だけ二歩を判定する。二歩では `ValueError` とし、盤面・手番・双方の持ち駒を変更しない。
- 判定操作と局面変更操作はすでに責務が分かれており、手番側の持ち駒選択や手番交代との共通化は不要な抽象化になるため、Refactorは行わなかった。

### レビュー

Refactor後に独立したコードレビューを行った。Critical・Importantはなし。Minorとして、二歩拒否テストのdocstringが「相手歩を誤って数えること」を検出するとしていたが、そのケースを直接テストしていないとの指摘があった。説明を実際に検証する内容へ修正し、対象テストを再実行して93件成功を確認した。

## 検証

- `python3 -m unittest discover -s tests -p 'test_movegen.py' -v`：Redでは93件中2失敗、Green後とMinor修正後は93件成功。
- `python3 -m unittest discover -s tests -v`：114件成功。
- `python3 -m kaname_shogi`：平手の初期配置と「手番：先手」を表示して終了。
- `git diff --check`：出力なし。

## 実装後の理解確認

### 第1問

先手番で、先手の持ち駒に歩が1枚あり、盤上の５七に先手の歩があるとする。空いている５五へその持ち歩を打とうとした場合、盤面・先手の持ち歩・手番はどうなるか。

本人の回答：盤面・先手の持ち歩・手番は変わらない。二歩となりエラーとなるため。

補足：正しい。二歩は `Hand.remove` より前に `ValueError` で拒否するため、盤面・先手の持ち歩・手番を含む局面全体が不変となる。

### 振り返り

本人の振り返り：ない。

補足：二歩の対象、拒否時の局面不変性、実装での検証順を理解できている。第27回の学習・設計・TDD実装・検証・Refactor後レビュー・理解確認は完了とする。

## mainへの取り込み

`codex/nifu` を `main` へfast-forwardで取り込んだ。取り込み後に `python3 -m unittest discover -s tests -v` を実行して114件成功、`python3 -m kaname_shogi` と `git diff --check` も成功を確認した。統合済みの `codex/nifu` ブランチは削除した。

## 今回は扱わないこと

行き所のない歩・香・桂、打ち歩詰め、成り・不成、王手・詰み・合法手判定、CLI入力、対局履歴は扱わない。
